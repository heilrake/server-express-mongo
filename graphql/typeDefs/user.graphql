type User {
  id: String
  email: String!
  password: String!
}

type Query {
  user(id: String!): User
  users: [User]
}

type Mutation {
  createUser(email: String!, password: String!): User
  deleteUser(id: String!): User
}